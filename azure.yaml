# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: PoRepoLineTracker
metadata:
  template: PoRepoLineTracker

services:
  api:
    project: ./src/PoRepoLineTracker.Api
    language: dotnet
    host: appservice
