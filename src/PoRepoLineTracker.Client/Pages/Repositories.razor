@page "/"
@page "/repositories"
@attribute [Authorize]
@implements IAsyncDisposable
@inject HttpClient Http
@inject ILogger<Repositories> Logger
@inject NavigationManager Navigation
@inject Radzen.DialogService DialogService
@inject Radzen.NotificationService NotificationService
@inject AnalysisFeedClient Feed
@inject UserPreferencesClient PreferencesClient
@using PoRepoLineTracker.Client.Services
@using PoRepoLineTracker.Client.Models
@using PoRepoLineTracker.Domain.Models
@using PoRepoLineTracker.Shared.Models.Dtos
@using PoRepoLineTracker.Shared.Models
@using Radzen
@using Radzen.Blazor
@using Microsoft.AspNetCore.Authorization

<div class="app-stack">
    <PageHero>
        <Heading>
            <RadzenStack Gap="0.125rem">
                <RadzenText TextStyle="TextStyle.H3" class="rp-title">Repositories</RadzenText>
                <RadzenText TextStyle="TextStyle.Body1" class="rp-muted-tight">
                    @if (pageState.IsLoadingRepositories)
                    {
                        <span>Loading your repositories...</span>
                    }
                    else
                    {
                        <span>Tracking @pageState.Repositories.Count @(pageState.Repositories.Count == 1 ? "repository" : "repositories")</span>
                    }
                </RadzenText>
            </RadzenStack>
        </Heading>
        <Actions>
            <RadzenButton Text="Upload Local"
                          Icon="upload_file"
                          ButtonStyle="ButtonStyle.Secondary"
                          Variant="Variant.Outlined"
                          Click="@ShowUploadDialog" />
            <RadzenButton Text="@(_showAddPanel ? "Cancel" : "Add Repository")"
                          Icon="@(_showAddPanel ? "close" : "add_circle")"
                          ButtonStyle="@(_showAddPanel ? ButtonStyle.Light : ButtonStyle.Primary)"
                          Variant="@(_showAddPanel ? Variant.Outlined : Variant.Filled)"
                          Click="@ToggleAddPanel" />
        </Actions>
    </PageHero>

    @* Inline Add Repository Panel *@
    @if (_showAddPanel)
    {
        <RadzenCard class="rp-inline-card">
            <AddRepository IsInline="true" OnInlineCompleted="@HandleAddCompleted" />
        </RadzenCard>
    }

    @* Notification Messages *@
    @if (!string.IsNullOrEmpty(pageState.AddRepoMessage))
    {
        <RadzenAlert AlertStyle="AlertStyle.Info" ShowIcon="true" AllowClose="true" Close="@(() => pageState.AddRepoMessage = string.Empty)">
            @pageState.AddRepoMessage
        </RadzenAlert>
    }
    @if (!string.IsNullOrEmpty(pageState.ErrorMessage))
    {
        <RadzenAlert AlertStyle="AlertStyle.Danger" ShowIcon="true" AllowClose="true" Close="@(() => pageState.ErrorMessage = string.Empty)">
            @pageState.ErrorMessage
        </RadzenAlert>
    }

    @* Live analysis feed. Renders nothing while idle, so it costs no vertical space until a job
       actually starts. *@
    <AnalysisActivityFeed />

    @* Repository Data Grid *@
    @if (pageState.IsLoadingRepositories)
    {
        <RadzenCard>
            <RadzenStack Gap="1rem">
                <RadzenSkeleton Width="200px" Height="24px" />
                <RadzenStack Gap="0.5rem">
                    @for (int i = 0; i < 5; i++)
                    {
                        <RadzenStack Orientation="Orientation.Horizontal" Gap="1rem" AlignItems="AlignItems.Center">
                            <RadzenSkeleton Circle="true" Width="40px" Height="40px" />
                            <RadzenSkeleton Width="150px" />
                            <RadzenSkeleton Width="200px" />
                            <RadzenSkeleton Width="120px" />
                            <RadzenSkeleton Width="80px" />
                        </RadzenStack>
                    }
                </RadzenStack>
            </RadzenStack>
        </RadzenCard>
    }
    else if (!pageState.Repositories.Any())
    {
        @* Animated onboarding empty state — step indicators + CTA *@
        <RadzenCard class="rp-onboarding-card">
            <RadzenStack AlignItems="AlignItems.Center" Gap="1.5rem">
                <RadzenStack Gap="0.5rem" AlignItems="AlignItems.Center">
                    <RadzenIcon Icon="code" class="rp-onboarding-icon" />
                    <RadzenText TextStyle="TextStyle.H5" class="rp-onboarding-title">No repositories yet</RadzenText>
                    <RadzenText TextStyle="TextStyle.Body1" class="rp-onboarding-lede">
                        Add your first GitHub repository and start tracking lines of code over time.
                    </RadzenText>
                </RadzenStack>

                @* How it works — 3-step flow *@
                <div class="rp-onboarding-steps">
                    <div class="onboarding-step">
                        <div class="onboarding-step-icon">
                            <RadzenIcon Icon="add_link" />
                        </div>
                        <RadzenText TextStyle="TextStyle.Subtitle2" class="rp-step-title">Add Repo</RadzenText>
                        <RadzenText TextStyle="TextStyle.Caption" class="rp-step-caption">Connect your GitHub repository</RadzenText>
                    </div>
                    <div class="onboarding-connector" aria-hidden="true"></div>
                    <div class="onboarding-step">
                        <div class="onboarding-step-icon">
                            <RadzenIcon Icon="manage_search" />
                        </div>
                        <RadzenText TextStyle="TextStyle.Subtitle2" class="rp-step-title">Auto-Analyze</RadzenText>
                        <RadzenText TextStyle="TextStyle.Caption" class="rp-step-caption">Commits scanned in background</RadzenText>
                    </div>
                    <div class="onboarding-connector" aria-hidden="true"></div>
                    <div class="onboarding-step">
                        <div class="onboarding-step-icon">
                            <RadzenIcon Icon="trending_up" />
                        </div>
                        <RadzenText TextStyle="TextStyle.Subtitle2" class="rp-step-title">Track Trends</RadzenText>
                        <RadzenText TextStyle="TextStyle.Caption" class="rp-step-caption">Charts, extensions & top files</RadzenText>
                    </div>
                </div>

                <RadzenButton Text="Add Your First Repository"
                              Icon="add_circle"
                              ButtonStyle="ButtonStyle.Primary"
                              class="onboarding-cta-pulse rp-onboarding-cta"
                              Click="@ToggleAddPanel" />
            </RadzenStack>
        </RadzenCard>
    }
    else
    {
        @* ─── Multi-repo comparison chart ───────────────────────────────────────
           No <ChartDisplayModeCard> here. It is a saved account-wide preference, not a property
           of this page, and rendering the control in two places meant two independent copies of
           the state: changing it in Settings left this page on the old mode until a reload. The
           value is read from UserPreferencesClient below and pushed by its Changed event. *@
        <AllReposComparisonChart @ref="_comparisonChart" ChartDisplayMode="@_chartDisplayMode" />

        @* ─── Stats cards ─────────────────────────────────────────────────────── *@
        <PortfolioStatTiles RepositoryCount="@pageState.Repositories.Count"
                            TotalLines="@_repoLatestLineCounts.Values.Sum()"
                            AnalyzedCount="@AnalyzedCount"
                            Trend="@TotalLinesTrend" />

        <RadzenCard>
            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0.5rem" class="rp-searchbar">
                <RadzenIcon Icon="search" class="rp-muted-icon" />
                <RadzenTextBox Value="@searchText" ValueChanged="@OnSearchTextChanged" Placeholder="Search owner or repository..." class="rp-search-input" />
                @if (!string.IsNullOrEmpty(searchText))
                {
                    <RadzenButton Icon="close" ButtonStyle="ButtonStyle.Light" Variant="Variant.Text" Size="ButtonSize.Small"
                                  Click="@(() => OnSearchTextChanged(string.Empty))" title="Clear search"
                                  aria-label="Clear repository search" />
                }
            </RadzenStack>

            @* Responsive="true" makes Radzen stack each row into a labelled card below its
               breakpoint. Without it the fixed column widths summed to ~736px, so on a phone the
               grid scrolled sideways and the Actions column sat permanently off-screen.

               RowRender is bound to OnRowRender rather than the inline lambda it used to carry:
               the lambda set only `cursor: pointer`, leaving rows unreachable by keyboard even
               though OnRowRender — which adds tabindex, role and Enter/Space handling — was
               already written and simply never wired up. *@
            <div class="u-scroll-x">
            <RadzenDataGrid @ref="grid"
                            Data="@FilteredRepositories"
                            TItem="RepositoryGridRow"
                            AllowFiltering="false"
                            AllowSorting="true"
                            AllowPaging="true"
                            PageSize="10"
                            PagerHorizontalAlign="HorizontalAlign.Center"
                            ShowPagingSummary="true"
                            PagingSummaryFormat="Showing {0} to {1} of {2} repositories"
                            AllowColumnResize="true"
                            Responsive="true"
                            RowRender="@OnRowRender"
                            RowClick="@OnRowClick"
                            Density="Density.Default"
                            class="rp-grid">
                <Columns>
                    <RadzenDataGridColumn TItem="RepositoryGridRow" Title="Repository" MinWidth="160px">
                        <Template Context="row">
                            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" Gap="0.35rem">
                                <RadzenText class="rp-repo-name" title="@($"{row.Owner}/{row.Name}")">@($"{row.Owner}/{row.Name}")</RadzenText>
                                <span @onclick:stopPropagation="true">
                                    <a href="@($"https://github.com/{row.Owner}/{row.Name}")" target="_blank"
                                       title="Open on GitHub"
                                       aria-label="@($"Open {row.Owner}/{row.Name} on GitHub in a new tab")"
                                       class="rp-github-link">
                                        @* RadzenIcon, not a bare <span class="material-icons">: the glyph
                                           comes from Radzen's bundled font rather than the Google Fonts
                                           CDN, so a failed CDN request cannot leave the literal text
                                           "open_in_new" in the middle of the grid row. *@
                                        <RadzenIcon Icon="open_in_new" class="rp-github-link__icon" />
                                    </a>
                                </span>
                            </RadzenStack>
                        </Template>
                    </RadzenDataGridColumn>
                    <RadzenDataGridColumn TItem="RepositoryGridRow" Property="TotalLinesSortValue" Title="Total Lines" Width="130px" TextAlign="TextAlign.Right" Sortable="true" Filterable="false">
                        <Template Context="row">
                            @if (row.TotalLinesSortValue.HasValue)
                            {
                                <RadzenText class="rp-lines-value">@row.TotalLinesSortValue.Value.ToString("N0")</RadzenText>
                            }
                            else if (row.LastAnalyzedCommitDate.HasValue)
                            {
                                <RadzenSkeleton Width="70px" Height="18px" />
                            }
                            else
                            {
                                <RadzenText class="rp-disabled">—</RadzenText>
                            }
                        </Template>
                    </RadzenDataGridColumn>
                    @* Trend column. The 45-point series comes from the /allcharts/365 response the
                       page already fetches for the Total Lines figures, so this adds a per-row
                       trend for zero extra network cost. Sortable="false": a sparkline has no
                       meaningful ordering, and the adjacent Total Lines column already sorts. *@
                    <RadzenDataGridColumn TItem="RepositoryGridRow" Title="Trend" Width="110px" Sortable="false" Filterable="false">
                        <Template Context="row">
                            @if (row.HasTrend)
                            {
                                <div class="rp-row-spark">
                                    <RadzenSparkline Data="@row.Trend"
                                                     CategoryProperty="Date"
                                                     ValueProperty="TotalLines"
                                                     Stroke="var(--rz-primary)"
                                                     Fill="rgba(103, 80, 164, 0.16)" />
                                </div>
                            }
                            else
                            {
                                <RadzenText class="rp-disabled">—</RadzenText>
                            }
                        </Template>
                    </RadzenDataGridColumn>
                    <RadzenDataGridColumn TItem="RepositoryGridRow" Property="LastAnalyzedCommitDate" Title="Last Analyzed" Width="130px" Sortable="true">
                        <Template Context="row">
                            @if (row.LastAnalyzedCommitDate.HasValue)
                            {
                                <RadzenStack Gap="0.1rem">
                                    <RadzenText class="rp-relative-time">@ChartFormat.RelativeTime(row.LastAnalyzedCommitDate)</RadzenText>
                                    <RadzenText TextStyle="TextStyle.Caption" class="rp-muted">@row.LastAnalyzedCommitDate.Value.ToString("MM/dd/yy HH:mm")</RadzenText>
                                </RadzenStack>
                            }
                            else
                            {
                                <RadzenBadge BadgeStyle="BadgeStyle.Light" Text="Never" />
                            }
                        </Template>
                    </RadzenDataGridColumn>
                    <RadzenDataGridColumn TItem="RepositoryGridRow" Title="Status" Width="200px" Filterable="false" Sortable="false">
                        <Template Context="row">
                            <AnalysisStatusCell Progress="@(_analysisProgress.TryGetValue(row.Id, out var p) ? p : null)"
                                                IsReanalyzing="@(reanalyzingRepositoryId == row.Id)"
                                                HasBeenAnalyzed="@row.LastAnalyzedCommitDate.HasValue"
                                                StartedUtc="@(_reanalysisStartTimes.TryGetValue(row.Id, out var st) ? st : null)" />
                        </Template>
                    </RadzenDataGridColumn>

                    <RadzenDataGridColumn TItem="RepositoryGridRow" Title="Actions" Width="80px" TextAlign="TextAlign.Center" Filterable="false" Sortable="false">
                        <Template Context="row">
                            <span @onclick:stopPropagation="true">
                                <RadzenStack Orientation="Orientation.Horizontal" Gap="0.25rem" JustifyContent="JustifyContent.Center">
                                    <RadzenButton Icon="refresh" Size="ButtonSize.ExtraSmall" ButtonStyle="ButtonStyle.Warning" Variant="Variant.Outlined"
                                                  Click="@(() => ConfirmReanalyze(row.Repository))"
                                                  title="Re-analyze from scratch"
                                                  aria-label="@($"Re-analyze {row.Owner}/{row.Name}")"
                                                  Disabled="@(reanalyzingRepositoryId == row.Id)" />
                                    <RadzenButton Icon="delete" Size="ButtonSize.ExtraSmall" ButtonStyle="ButtonStyle.Danger" Variant="Variant.Outlined"
                                                  Click="@(() => ConfirmDeleteRepository(row.Repository))"
                                                  title="Delete repository"
                                                  aria-label="@($"Delete {row.Owner}/{row.Name}")" />
                                </RadzenStack>
                            </span>
                        </Template>
                    </RadzenDataGridColumn>
                    <RadzenDataGridColumn TItem="RepositoryGridRow" Title="" Width="36px" Filterable="false" Sortable="false">
                        <Template>
                            <RadzenIcon Icon="chevron_right" class="rp-chevron" />
                        </Template>
                    </RadzenDataGridColumn>
                </Columns>
            </RadzenDataGrid>
            </div>

            @* Danger Zone — separated from primary actions *@
            <RadzenStack Orientation="Orientation.Horizontal" AlignItems="AlignItems.Center" JustifyContent="JustifyContent.SpaceBetween"
                         class="rp-danger-zone">
                <RadzenText TextStyle="TextStyle.Caption" class="rp-danger-zone__label">Danger Zone</RadzenText>
                <RadzenButton Text="Remove All Repositories"
                              Icon="delete_forever"
                              ButtonStyle="ButtonStyle.Danger"
                              Variant="Variant.Outlined"
                              Size="ButtonSize.Small"
                              Click="@ConfirmRemoveAll"
                              IsBusy="@isRemovingAll"
                              BusyText="Removing..." />
            </RadzenStack>
        </RadzenCard>
    }
</div>



@code {
    [Parameter, SupplyParameterFromQuery(Name = "q")] public string? Q { get; set; }

    private RepositoryPageState pageState = new();
    private RadzenDataGrid<RepositoryGridRow>? grid;
    private AllReposComparisonChart? _comparisonChart;
    private bool _chartPrimed;
    private RepositoryId? reanalyzingRepositoryId;
    private bool _showAddPanel = false;
    private bool isRemovingAll = false;
    private string searchText = string.Empty;
    // Tracks the last Q value we wrote to the URL ourselves, so OnParametersSet
    // does not overwrite searchText after our own NavigateTo call.
    private string? _lastSyncedQ;
    private Dictionary<RepositoryId, DateTime> _reanalysisStartTimes = new();
    private Dictionary<RepositoryId, int> _repoLatestLineCounts = new();

    /// <summary>
    /// Per-repository daily totals for the grid sparkline column. Built from the SAME
    /// /allcharts/365 response that already feeds the line-count figures, so a trend beside every
    /// row costs no additional request — the series was being fetched and discarded.
    /// Trimmed to the last 45 points: a ~90px sparkline cannot resolve more.
    /// </summary>
    private Dictionary<RepositoryId, IReadOnlyList<DailyLineCountDto>> _repoTrends = new();
    private Dictionary<RepositoryId, AnalysisProgressDto> _analysisProgress = new();
    private readonly CancellationTokenSource _disposeCts = new();
    private ChartDisplayMode _chartDisplayMode = ChartDisplayMode.TrueData;

    private IEnumerable<RepositoryGridRow> FilteredRepositories =>
        string.IsNullOrWhiteSpace(searchText)
            ? RepositoryRows
            : RepositoryRows.Where(r =>
                r.Owner.Contains(searchText, StringComparison.OrdinalIgnoreCase) ||
                r.Name.Contains(searchText, StringComparison.OrdinalIgnoreCase));

    private IEnumerable<RepositoryGridRow> RepositoryRows => pageState.Repositories.Select(repository => new RepositoryGridRow(
        repository,
        _repoLatestLineCounts.TryGetValue(repository.Id, out var totalLines) ? totalLines : null,
        _repoTrends.TryGetValue(repository.Id, out var trend) ? trend : []));

    private int AnalyzedCount => pageState.Repositories.Count(r => r.LastAnalyzedCommitDate.HasValue);

    /// <summary>
    /// Portfolio-wide daily totals for the Total Lines sparkline. Built from the history already
    /// fetched by <see cref="LoadLatestLineCounts"/>, so the trend costs no extra request.
    /// Capped at the most recent 60 points — a sparkline a few centimetres wide cannot resolve
    /// 365 and the extra nodes are pure render cost.
    /// </summary>
    private List<DailyLineCountDto> TotalLinesTrend { get; set; } = [];


    protected override void OnParametersSet()
    {
        // Only sync searchText from the URL query param when the value genuinely
        // changed from outside (e.g., the user typed a URL directly or the browser
        // navigated back/forward).  When we call NavigateTo ourselves inside
        // OnSearchTextChanged we pre-set _lastSyncedQ so this guard fires and we
        // do NOT overwrite the value the user is actively typing.
        if (Q != _lastSyncedQ)
        {
            _lastSyncedQ = Q;
            searchText = Q ?? string.Empty;
        }
    }

    protected override async Task OnInitializedAsync()
    {
        Feed.ProgressReceived += OnAnalysisProgressAsync;
        PreferencesClient.Changed += OnPreferencesChangedAsync;

        await Task.WhenAll(
            LoadRepositories(),
            LoadLatestLineCounts(),
            LoadChartDisplayMode(),
            Feed.EnsureConnectedAsync());

        StartFallbackPollIfNeeded();
    }

    /// <summary>
    /// Applies a pushed progress frame to the grid.
    ///
    /// <para>This is the same state the poll used to fetch, arriving as it happens rather than up
    /// to five seconds later — and without the request-per-pending-repository that produced it.
    /// The completion branch does the reload the poll used to do, which is why the poll can be
    /// skipped entirely while the hub is up.</para>
    /// </summary>
    private async Task OnAnalysisProgressAsync(AnalysisProgressDto progress)
    {
        var wasRunning = _analysisProgress.TryGetValue(progress.RepositoryId, out var previous) && previous.IsRunning;
        _analysisProgress[progress.RepositoryId] = progress;

        if (progress.IsRunning)
        {
            reanalyzingRepositoryId ??= progress.RepositoryId;
            await InvokeAsync(StateHasChanged);
            return;
        }

        // Only act on the transition. The service pushes the terminal frame once, but a reconnect
        // can replay state, and re-running the reload on each replay would restart the chart
        // fetches for a job that finished minutes ago.
        if (!wasRunning)
        {
            await InvokeAsync(StateHasChanged);
            return;
        }

        if (reanalyzingRepositoryId == progress.RepositoryId) reanalyzingRepositoryId = null;
        _reanalysisStartTimes.Remove(progress.RepositoryId);
        _awaitingAnalysis.Remove(progress.RepositoryId);

        if (progress.ErrorMessage is null)
        {
            _analysisProgress.Remove(progress.RepositoryId);

            await LoadRepositories();
            await LoadLatestLineCounts();
            await ReloadComparisonChartAsync();

            NotificationService.Notify(new NotificationMessage
            {
                Severity = NotificationSeverity.Success,
                Summary = "Analysis Complete",
                Detail = $"'{progress.Owner}/{progress.Name}' has been analyzed.",
                Duration = 5000
            });
        }

        await InvokeAsync(StateHasChanged);
        if (grid is not null) await InvokeAsync(grid.Reload);
    }

    /// <summary>
    /// Kicks off the growth chart's first fetch once the page has painted.
    /// <para>
    /// AllReposComparisonChart deliberately does not load in OnInitializedAsync so the request
    /// stays off the initial render path, and documents that the parent primes it afterwards —
    /// but no parent ever did. The card therefore always rendered its "No history data yet"
    /// empty state until the user happened to click a range button, even with repositories
    /// analysed and the very same endpoint already supplying the Total Lines figure above it.
    /// </para>
    /// </summary>
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        // Not gated on firstRender: the chart lives inside the "repositories loaded" branch, so
        // on the very first pass the page is still showing its skeleton and @ref is null. Prime
        // on the first render where the ref actually exists, and only once.
        if (_chartPrimed || _comparisonChart is null) return;
        _chartPrimed = true;
        await _comparisonChart.PrimeAsync();
    }

    private async Task LoadChartDisplayMode()
    {
        try
        {
            var preferences = await PreferencesClient.GetAsync();
            _chartDisplayMode = preferences?.ChartDisplayMode ?? ChartDisplayMode.TrueData;
        }
        catch (Exception ex)
        {
            Logger.LogWarning(ex, "Could not load chart display mode; defaulting to true data");
            _chartDisplayMode = ChartDisplayMode.TrueData;
        }
    }

    /// <summary>
    /// Applies a preference saved anywhere in the app — in practice, the card on the Settings
    /// page — without a reload. The comparison chart re-transforms from data it already holds,
    /// so this costs no request.
    /// </summary>
    private async Task OnPreferencesChangedAsync(UserPreferences preferences)
    {
        if (_chartDisplayMode == preferences.ChartDisplayMode) return;
        _chartDisplayMode = preferences.ChartDisplayMode;
        await InvokeAsync(StateHasChanged);
    }

    private async Task LoadLatestLineCounts()
    {
        try
        {
            var all = await Http.GetAppJsonAsync("/api/repositories/allcharts/365", AppJsonSerializerContext.Default.ListRepositoryLineCountHistoryDto);
            if (all != null)
            {
                // r.TotalLines, NOT the last point of the windowed series. The server computes it
                // from every commit the repository has (see RepositoryTotals), so a repository
                // untouched for longer than the 365-day window still reports its real size here.
                // Reading it off the series is what made this page's "Total Lines" smaller than
                // the identically-labelled figure on Insights.
                _repoLatestLineCounts = all
                    .Where(r => r.TotalLines > 0)
                    .ToDictionary(r => r.RepositoryId, r => r.TotalLines);
                _repoTrends = all
                    .Where(r => r.DailyLineCounts.Any())
                    .ToDictionary(
                        r => r.RepositoryId,
                        r => (IReadOnlyList<DailyLineCountDto>)r.DailyLineCounts
                            .OrderBy(d => d.Date)
                            .TakeLast(45)
                            .ToList());

                Logger.LogInformation("Loaded latest line counts for {Count} repositories", _repoLatestLineCounts.Count);

                // Sum every repo's per-day total into one portfolio series for the sparkline.
                TotalLinesTrend = all
                    .SelectMany(r => r.DailyLineCounts)
                    .GroupBy(d => d.Date.Date)
                    .OrderBy(g => g.Key)
                    .Select(g => new DailyLineCountDto { Date = g.Key, TotalLines = g.Sum(d => d.TotalLines) })
                    .TakeLast(60)
                    .ToList();
            }
        }
        catch (Exception ex)
        {
            Logger.LogWarning(ex, "Could not load latest line counts — Total Lines column will show dashes");
        }
    }

    private void OnSearchTextChanged(string value)
    {
        searchText = value;
        // Pre-sync _lastSyncedQ so that the OnParametersSet triggered by NavigateTo
        // does not overwrite the value the user just typed.
        _lastSyncedQ = string.IsNullOrEmpty(value) ? null : value;
        var uri = Navigation.GetUriWithQueryParameter("q", string.IsNullOrEmpty(value) ? null : value);
        Navigation.NavigateTo(uri, replace: true);
    }

    private void OnRowClick(DataGridRowMouseEventArgs<RepositoryGridRow> args)
    {
        Navigation.NavigateTo($"/repositories/{args.Data.Id}");
    }

    /// <summary>
    /// Adds tabindex + role="button" + aria-label + Enter/Space keyboard activation
    /// to each row so screen readers and keyboard users can navigate the repo
    /// list without falling back to the chevron column. (#5 + #9)
    /// </summary>
    private void OnRowRender(RowRenderEventArgs<RepositoryGridRow> args)
    {
        args.Attributes["style"] = "cursor: pointer;";
        args.Attributes["tabindex"] = "0";
        args.Attributes["role"] = "button";
        args.Attributes["aria-label"] = $"Open repository {args.Data.Owner}/{args.Data.Name}";
        args.Attributes["onkeydown"] = "if(event.key==='Enter'||event.key===' '){event.preventDefault();this.click();}";
    }

    // ToRelativeTime moved to ChartFormat — it was duplicated verbatim on RepositoryDetail.

    private void ToggleAddPanel()
    {
        _showAddPanel = !_showAddPanel;
    }

    private async Task ShowUploadDialog()
    {
        await DialogService.OpenAsync<UploadRepository>("Upload Local Repository",
            new Dictionary<string, object> { },
            new DialogOptions
            {
                Width = "500px",
                Height = "auto",
                Resizable = true,
                Draggable = true
            });
    }

    private async Task HandleAddCompleted()
    {
        _showAddPanel = false;
        await LoadRepositories();
        await LoadLatestLineCounts();
        await ReloadComparisonChartAsync();
        StartFallbackPollIfNeeded();
        StateHasChanged();
    }

    /// <summary>
    /// Reloads the "All Repositories — Growth Overview" chart on the UI context.
    /// No-op until the chart is rendered (it only exists once repositories load).
    /// </summary>
    private Task ReloadComparisonChartAsync() =>
        _comparisonChart is null ? Task.CompletedTask : InvokeAsync(_comparisonChart.ReloadAsync);

    private async Task LoadRepositories()
    {
        pageState.IsLoadingRepositories = true;
        pageState.ClearMessages();
        
        try
        {
            var repositories = await Http.GetAppJsonAsync("/api/repositories", AppJsonSerializerContext.Default.ListGitHubRepository);
            pageState.Repositories = repositories ?? new List<GitHubRepository>();
            Logger.LogInformation("Loaded {Count} repositories.", pageState.Repositories.Count);
        }
        catch (Exception ex)
        {
            Logger.LogError(ex, "Error loading repositories: {Message}", ex.Message);
            pageState.ErrorMessage = $"Error loading repositories: {ex.Message}";
        }
        finally
        {
            pageState.IsLoadingRepositories = false;
        }
    }

    private async Task ConfirmDeleteRepository(GitHubRepository repo)
    {
        var confirmed = await DialogService.Confirm(
            $"Are you sure you want to delete '{repo.Owner}/{repo.Name}'? This will remove all associated data.",
            "Delete Repository",
            new ConfirmOptions { OkButtonText = "Delete", CancelButtonText = "Cancel" });

        if (confirmed == true)
        {
            await DeleteRepository(repo.Id);
        }
    }

    private async Task ConfirmReanalyze(GitHubRepository repo)
    {
        var confirmed = await DialogService.Confirm(
            $"Re-analyze '{repo.Owner}/{repo.Name}' from scratch?\n\nThis will delete all existing commit data and re-analyze the entire repository using your current file extension settings. This may take some time for repositories with many commits.",
            "Re-analyze Repository",
            new ConfirmOptions { OkButtonText = "Re-analyze", CancelButtonText = "Cancel" });

        if (confirmed == true)
        {
            await ReanalyzeRepository(repo);
        }
    }

    private async Task ReanalyzeRepository(GitHubRepository repo)
    {
        var priorDate = repo.LastAnalyzedCommitDate; // baseline for polling
        bool successfullyQueued = false;

        try
        {
            reanalyzingRepositoryId = repo.Id;
            _reanalysisStartTimes[repo.Id] = DateTime.UtcNow;
            StateHasChanged();

            Logger.LogInformation("Re-analyzing repository {RepositoryId}", repo.Id);
            var response = await Http.PostAsync($"/api/repositories/{repo.Id}/reanalyze", null);

            if (response.IsSuccessStatusCode)
            {
                successfullyQueued = true;
                NotificationService.Notify(new NotificationMessage
                {
                    Severity = NotificationSeverity.Success,
                    Summary = "Re-analysis Started",
                    Detail = $"Re-analyzing '{repo.Owner}/{repo.Name}' with your current file extension settings. The badge will clear automatically when complete.",
                    Duration = 6000
                });
                Logger.LogInformation("Re-analysis queued for repository {RepositoryId}", repo.Id);

                // Register the wait and let the one fallback loop cover it. It no-ops while the
                // hub is connected, and the hub's frames clear the entry through the same handler.
                _awaitingAnalysis[repo.Id] = priorDate;
                StartFallbackPollIfNeeded();
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                NotificationService.Notify(new NotificationMessage
                {
                    Severity = NotificationSeverity.Error,
                    Summary = "Error",
                    Detail = $"Failed to start re-analysis: {response.StatusCode}",
                    Duration = 6000
                });
                Logger.LogError("Failed to re-analyze repository {RepositoryId}. Status: {StatusCode}", repo.Id, response.StatusCode);
            }
        }
        catch (Exception ex)
        {
            NotificationService.Notify(new NotificationMessage
            {
                Severity = NotificationSeverity.Error,
                Summary = "Error",
                Detail = $"Error starting re-analysis: {ex.Message}",
                Duration = 6000
            });
            Logger.LogError(ex, "Error re-analyzing repository {RepositoryId}", repo.Id);
        }
        finally
        {
            // Only clear the badge immediately if the API call failed — on success polling handles cleanup
            if (!successfullyQueued)
            {
                reanalyzingRepositoryId = null;
                _reanalysisStartTimes.Remove(repo.Id);
                _awaitingAnalysis.Remove(repo.Id);
                StateHasChanged();
            }
        }
    }

    // ─── Fallback poll ──────────────────────────────────────────────────────────
    //
    // ONE loop, and it does not decide anything. It turns whatever the server says into the same
    // AnalysisProgressDto frames the hub pushes and hands them to OnAnalysisProgressAsync — so
    // "a job finished" is handled in exactly one place whether the news arrived by push or by
    // poll.
    //
    // It replaces two loops that each re-implemented that handling: a 15-second poll for
    // repositories that had never been analysed, and a 5-second poll started per re-analysis.
    // Between them and the hub handler, "reload repositories, reload line counts, reload the
    // comparison chart, clear the badge, notify" was written out three times, guarded by
    // Feed.IsConnected checks in four separate methods. They had already drifted: only the hub
    // path removed the entry from _analysisProgress on success, and only the re-analysis poll
    // cleared the badge on its own timeout.

    /// <summary>
    /// Repositories whose analysis this page is waiting on, each with the LastAnalyzedCommitDate
    /// it had when the wait began — that value changing is the completion signal.
    /// </summary>
    private readonly Dictionary<RepositoryId, DateTime?> _awaitingAnalysis = [];

    private bool _fallbackPollRunning;

    /// <summary>
    /// Starts the fallback loop if anything is being waited on and the hub is not carrying it.
    ///
    /// <para>The hub reports every step of every job the user owns, so polling on top of it would
    /// fetch the same data twice. This exists for the cases the hub cannot cover: a proxy that
    /// blocks WebSockets, or a failed handshake.</para>
    /// </summary>
    private void StartFallbackPollIfNeeded()
    {
        foreach (var pending in pageState.Repositories.Where(r => r.LastAnalyzedCommitDate is null))
            _awaitingAnalysis.TryAdd(pending.Id, null);

        if (_fallbackPollRunning) return;
        if (Feed.IsConnected) return;
        if (_awaitingAnalysis.Count == 0) return;

        _ = FallbackPollAsync();
    }

    private async Task FallbackPollAsync()
    {
        _fallbackPollRunning = true;

        using var cts = CancellationTokenSource.CreateLinkedTokenSource(_disposeCts.Token);
        // Hard stop. A very large repository must not leave a timer running for the whole session.
        cts.CancelAfter(TimeSpan.FromMinutes(60));

        using var timer = new PeriodicTimer(TimeSpan.FromSeconds(5));
        Logger.LogInformation("Fallback analysis poll started for {Count} repositories", _awaitingAnalysis.Count);

        try
        {
            while (_awaitingAnalysis.Count > 0 && await timer.WaitForNextTickAsync(cts.Token))
            {
                // The hub may come back mid-job — a reconnect is exactly what WithAutomaticReconnect
                // is for. Stand down when it does rather than duplicating its frames.
                if (Feed.IsConnected)
                {
                    Logger.LogInformation("Analysis feed reconnected — stopping fallback poll");
                    break;
                }

                try
                {
                    await PollTickAsync(cts.Token);
                }
                catch (OperationCanceledException) { throw; }
                catch (Exception ex)
                {
                    Logger.LogWarning(ex, "Fallback poll tick failed — will retry");
                }
            }
        }
        catch (OperationCanceledException)
        {
            Logger.LogInformation("Fallback analysis poll cancelled");
        }
        finally
        {
            _fallbackPollRunning = false;
        }
    }

    private async Task PollTickAsync(CancellationToken cancellationToken)
    {
        var repos = await Http.GetAppJsonAsync("/api/repositories", AppJsonSerializerContext.Default.ListGitHubRepository, cancellationToken);
        if (repos is null) return;

        foreach (var (repositoryId, baseline) in _awaitingAnalysis.ToList())
        {
            var updated = repos.FirstOrDefault(r => r.Id == repositoryId);

            // Deleted from under us — stop waiting rather than polling a 404 for an hour.
            if (updated is null)
            {
                _awaitingAnalysis.Remove(repositoryId);
                continue;
            }

            // A changed LastAnalyzedCommitDate is the completion signal. Checked BEFORE the
            // progress endpoint because a finished job's progress entry expires, and reading
            // "no progress" as "still working" is what used to leave badges spinning.
            if (updated.LastAnalyzedCommitDate is not null && updated.LastAnalyzedCommitDate != baseline)
            {
                var index = pageState.Repositories.FindIndex(r => r.Id == repositoryId);
                if (index >= 0) pageState.Repositories[index] = updated;

                await PublishTerminalFrameAsync(repositoryId, error: null);
                continue;
            }

            // Still in flight. Mirror the running frame the hub would have pushed, so the step
            // name, commit counts and stuck/error flags reach the grid by the same route.
            AnalysisProgressDto? progress = null;
            try
            {
                progress = await Http.GetAppJsonAsync(
                    $"/api/repositories/{repositoryId}/analysis-progress",
                    AppJsonSerializerContext.Default.AnalysisProgressDto,
                    cancellationToken);
            }
            catch (OperationCanceledException) { throw; }
            catch
            {
                // 404 until the job registers itself. Not an error — just nothing to report yet.
            }

            if (progress is null) continue;

            if (progress.IsRunning || progress.ErrorMessage is not null)
            {
                await OnAnalysisProgressAsync(progress);
                if (!progress.IsRunning) _awaitingAnalysis.Remove(repositoryId);
            }

            // A terminal frame with no error and no date change yet means the write has not
            // landed. Leave it outstanding and pick the completion up on a later tick.
        }
    }

    /// <summary>
    /// Synthesises the terminal frame the hub would have pushed and routes it through the one
    /// handler, so completion does the same work by either path.
    /// </summary>
    private async Task PublishTerminalFrameAsync(RepositoryId repositoryId, string? error)
    {
        _awaitingAnalysis.Remove(repositoryId);

        var repo = pageState.Repositories.FirstOrDefault(r => r.Id == repositoryId);

        // The handler only acts on a running → finished transition, so a repository never seen
        // running needs its running frame first. Without this, the completion of a job the poll
        // only ever caught at the end would be silently dropped.
        if (!(_analysisProgress.TryGetValue(repositoryId, out var seen) && seen.IsRunning))
        {
            await OnAnalysisProgressAsync(new AnalysisProgressDto
            {
                RepositoryId = repositoryId,
                Owner = repo?.Owner ?? string.Empty,
                Name = repo?.Name ?? string.Empty,
                IsRunning = true,
                StepName = "Analyzing",
                StepDescription = "Working…"
            });
        }

        await OnAnalysisProgressAsync(new AnalysisProgressDto
        {
            RepositoryId = repositoryId,
            Owner = repo?.Owner ?? string.Empty,
            Name = repo?.Name ?? string.Empty,
            IsRunning = false,
            StepName = error is null ? "Complete" : "Failed",
            StepDescription = error ?? "Analysis complete",
            ErrorMessage = error
        });
    }

    public async ValueTask DisposeAsync()
    {
        // Unsubscribe only. AnalysisFeedClient is container-owned and shared with the activity
        // feed, so disposing it here would take the feed's connection down with the page.
        Feed.ProgressReceived -= OnAnalysisProgressAsync;
        PreferencesClient.Changed -= OnPreferencesChangedAsync;

        await _disposeCts.CancelAsync();
        _disposeCts.Dispose();
    }

    private sealed record RepositoryGridRow(
        GitHubRepository Repository,
        int? TotalLinesSortValue,
        IReadOnlyList<DailyLineCountDto> Trend)
    {
        public RepositoryId Id => Repository.Id;
        public string Owner => Repository.Owner;
        public string Name => Repository.Name;
        public DateTime? LastAnalyzedCommitDate => Repository.LastAnalyzedCommitDate;

        /// <summary>A sparkline of one or two points is a dot, not a trend — hide it below three.</summary>
        public bool HasTrend => Trend.Count > 2;
    }

    private async Task DeleteRepository(RepositoryId repositoryId)
    {
        try
        {
            Logger.LogInformation("Deleting repository {RepositoryId}", repositoryId);
            var response = await Http.DeleteAsync($"/api/repositories/{repositoryId}");

            if (response.IsSuccessStatusCode)
            {
                var repoToRemove = pageState.Repositories.FirstOrDefault(r => r.Id == repositoryId);
                if (repoToRemove != null)
                {
                    pageState.Repositories.Remove(repoToRemove);
                    NotificationService.Notify(new NotificationMessage
                    {
                        Severity = NotificationSeverity.Success,
                        Summary = "Deleted",
                        Detail = $"Repository '{repoToRemove.Owner}/{repoToRemove.Name}' deleted successfully!",
                        Duration = 4000
                    });
                }

                if (grid is not null)
                    await grid.Reload();
                Logger.LogInformation("Repository {RepositoryId} deleted successfully", repositoryId);
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                NotificationService.Notify(new NotificationMessage
                {
                    Severity = NotificationSeverity.Error,
                    Summary = "Error",
                    Detail = $"Failed to delete repository: {response.StatusCode}",
                    Duration = 6000
                });
                Logger.LogError("Failed to delete repository {RepositoryId}. Status: {StatusCode}", repositoryId, response.StatusCode);
            }
        }
        catch (Exception ex)
        {
            NotificationService.Notify(new NotificationMessage
            {
                Severity = NotificationSeverity.Error,
                Summary = "Error",
                Detail = $"Error deleting repository: {ex.Message}",
                Duration = 6000
            });
            Logger.LogError(ex, "Error deleting repository {RepositoryId}: {Message}", repositoryId, ex.Message);
        }
    }

    private async Task ConfirmRemoveAll()
    {
        var confirmed = await DialogService.Confirm(
            "Are you sure you want to remove ALL repositories? This action cannot be undone.",
            "Remove All Repositories",
            new ConfirmOptions { OkButtonText = "Remove All", CancelButtonText = "Cancel" });

        if (confirmed == true)
        {
            await RemoveAllRepositories();
        }
    }

    private async Task RemoveAllRepositories()
    {
        try
        {
            isRemovingAll = true;
            Logger.LogInformation("Starting removal of all repositories via API endpoint.");
            
            var response = await Http.DeleteAsync("/api/repositories/all");
            
            if (response.IsSuccessStatusCode)
            {
                pageState.Repositories.Clear();

                NotificationService.Notify(new NotificationMessage
                {
                    Severity = NotificationSeverity.Success,
                    Summary = "Success",
                    Detail = "All repositories and data have been successfully removed!",
                    Duration = 4000
                });
                Logger.LogInformation("All repositories removed successfully.");
            }
            else
            {
                var errorContent = await response.Content.ReadAsStringAsync();
                NotificationService.Notify(new NotificationMessage
                {
                    Severity = NotificationSeverity.Error,
                    Summary = "Error",
                    Detail = $"Error removing repositories: {response.StatusCode}",
                    Duration = 6000
                });
                Logger.LogError("Failed to remove all repositories. Status: {StatusCode}", response.StatusCode);
            }
        }
        catch (Exception ex)
        {
            NotificationService.Notify(new NotificationMessage
            {
                Severity = NotificationSeverity.Error,
                Summary = "Error",
                Detail = $"Error removing all repositories: {ex.Message}",
                Duration = 6000
            });
            Logger.LogError(ex, "Error removing all repositories: {Message}", ex.Message);
        }
        finally
        {
            isRemovingAll = false;
        }
    }
}
